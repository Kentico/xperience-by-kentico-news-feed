import classNames from 'classnames';

/**
 * Utility for conditionally joining classNames together.
 * Re-exported from the classnames package for design system component use.
 */
export const cn = classNames;
